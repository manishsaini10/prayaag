<?php

namespace Tests\Fixtures;

/**
 * RETIRED fixture (multi-tenancy removed — single-site CMS).
 *
 * Previously a tenant-owned model used by TenantScopeTest to exercise the
 * global tenant scope. Nothing references it now. Kept only because the tooling
 * cannot delete the file; safe to remove from the repo.
 */
class Note
{
}
